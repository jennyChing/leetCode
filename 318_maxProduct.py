class Solution(object):
    def maxProduct(self, words):
        """
        :type words: List[str]
        :rtype: int
        use 32 bit to represent every 2 strings in 26 characters and use & operator to see if contains any same character
        """
        max_product = 0
        for
